# NEVER commit a real version of this file.
#
# Create the actual Secret on the cluster via kubectl, not by applying this YAML:
#
#   kubectl create secret generic khata-secrets -n khata \
#     --from-literal=POSTGRES_PASSWORD='<random 32-byte hex>' \
#     --from-literal=TELEGRAM_BOT_TOKEN='<from BotFather>' \
#     --from-literal=TELEGRAM_WEBHOOK_SECRET='<random 32-char string>' \
#     --from-literal=ALLOWED_TELEGRAM_USER_IDS='<comma-separated numeric IDs>' \
#     --from-literal=SESSION_SECRET='<openssl rand -hex 32>' \
#     --from-literal=OPENROUTER_API_KEY='sk-or-v1-...' \
#     --from-literal=S3_ENDPOINT='https://<account>.r2.cloudflarestorage.com' \
#     --from-literal=S3_BUCKET='khata-statements' \
#     --from-literal=S3_REGION='auto' \
#     --from-literal=S3_ACCESS_KEY_ID='<R2 access key>' \
#     --from-literal=S3_SECRET_ACCESS_KEY='<R2 secret key>'
#
# This file is committed only as a *shape reference* — the literal values
# below are placeholders and must NOT be deployed.
apiVersion: v1
kind: Secret
metadata:
  name: khata-secrets
  namespace: khata
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  TELEGRAM_BOT_TOKEN: "REPLACE_ME"
  TELEGRAM_WEBHOOK_SECRET: "REPLACE_ME"
  ALLOWED_TELEGRAM_USER_IDS: "REPLACE_ME"
  SESSION_SECRET: "REPLACE_ME_64_HEX_CHARS"
  OPENROUTER_API_KEY: "sk-or-v1-REPLACE_ME"
  S3_ENDPOINT: "https://REPLACE.r2.cloudflarestorage.com"
  S3_BUCKET: "khata-statements"
  S3_REGION: "auto"
  S3_ACCESS_KEY_ID: "REPLACE_ME"
  S3_SECRET_ACCESS_KEY: "REPLACE_ME"
